name: Code Formatting & Analyzers

on:
  push:
    branches: [ main, develop, 'feature/**', 'bugfix/**' ]
  pull_request:
    branches: [ main, develop ]
  workflow_dispatch: # Allow manual triggering

env:
  DOTNET_VERSION: '9.0.x'
  DOTNET_SKIP_FIRST_TIME_EXPERIENCE: true
  DOTNET_CLI_TELEMETRY_OPTOUT: true

jobs:
  code-formatting-and-analyzers:
    name: Code Formatting & Analyzers
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 1

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}

    - name: Cache NuGet packages
      uses: actions/cache@v4
      with:
        path: ~/.nuget/packages
        key: ${{ runner.os }}-nuget-${{ hashFiles('Directory.Packages.props', '**/*.csproj') }}
        restore-keys: |
          ${{ runner.os }}-nuget-

    - name: Restore dependencies
      run: dotnet restore TS4Tools.sln

    - name: Check code formatting
      run: |
        Write-Host "🔍 Checking code formatting with dotnet format..."
        Write-Host "ℹ️ This will verify that all code follows the project's formatting standards."

        dotnet format TS4Tools.sln --verify-no-changes --verbosity diagnostic

        if ($LASTEXITCODE -ne 0) {
          Write-Host ""
          Write-Error "❌ Code formatting issues detected!"
          Write-Host ""
          Write-Host "🔧 To fix formatting issues locally:"
          Write-Host "   dotnet format TS4Tools.sln"
          Write-Host ""
          Write-Host "💡 Consider setting up a pre-commit hook to automatically format code:"
          Write-Host "   git config core.hooksPath .githooks"
          Write-Host ""
          exit 1
        }

        Write-Host "✅ All files are properly formatted!"

    - name: Run .NET analyzers
      run: |
        Write-Host "🔍 Running .NET code analyzers..."
        Write-Host "ℹ️ This includes Microsoft.CodeAnalysis.NetAnalyzers and SonarAnalyzer.CSharp"

        # Build with analyzer warnings treated as errors
        dotnet build TS4Tools.sln --no-restore --configuration Release --verbosity normal --property:TreatWarningsAsErrors=true --property:WarningsAsErrors=""

        if ($LASTEXITCODE -ne 0) {
          Write-Host ""
          Write-Error "❌ Code analyzer warnings detected!"
          Write-Host ""
          Write-Host "🔧 To see detailed analyzer output:"
          Write-Host "   dotnet build --verbosity normal"
          Write-Host ""
          Write-Host "💡 Common fixes:"
          Write-Host "   • Make unused private members internal or remove them"
          Write-Host "   • Add null checks where required"
          Write-Host "   • Follow naming conventions (PascalCase for public, camelCase for private)"
          Write-Host "   • Add XML documentation for public APIs"
          Write-Host ""
          exit 1
        }

        Write-Host "✅ All analyzer checks passed!"

    - name: Run EditorConfig validation
      run: |
        Write-Host "🔍 Validating EditorConfig compliance..."

        # Check if .editorconfig exists
        if (-not (Test-Path ".editorconfig")) {
          Write-Warning "⚠️ No .editorconfig file found in repository root"
        } else {
          Write-Host "✅ .editorconfig file found"

          # Show EditorConfig settings being applied
          Write-Host "ℹ️ EditorConfig settings:"
          Get-Content ".editorconfig" | Select-Object -First 20
        }

    - name: Summary
      if: success()
      run: |
        Write-Host ""
        Write-Host "🎉 All code quality checks passed!" -ForegroundColor Green
        Write-Host ""
        Write-Host "✅ Code formatting: PASSED" -ForegroundColor Green
        Write-Host "✅ .NET analyzers: PASSED" -ForegroundColor Green
        Write-Host "✅ EditorConfig: VALIDATED" -ForegroundColor Green
        Write-Host ""
        Write-Host "🚀 Code is ready for review and merge!"

  # Optional: Create a summary comment on PRs
  pr-comment:
    name: PR Comment
    runs-on: ubuntu-latest
    needs: code-formatting-and-analyzers
    if: github.event_name == 'pull_request' && success()

    steps:
    - name: Comment PR
      uses: actions/github-script@v7
      with:
        script: |
          github.rest.issues.createComment({
            issue_number: context.issue.number,
            owner: context.repo.owner,
            repo: context.repo.repo,
            body: `## ✅ Code Quality Check Passed

            All code formatting and analyzer checks have passed successfully!

            - ✅ **Code Formatting**: All files properly formatted
            - ✅ **.NET Analyzers**: No warnings detected
            - ✅ **EditorConfig**: Configuration validated

            Your code follows the project's quality standards. Great job! 🎉`
          })
